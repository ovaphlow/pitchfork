# 后端服务基址（跨域访问：浏览器直接请求该绝对地址，后端需自行配置 CORS 允许头）
# 后端端口以 docs/architecture.md 最终登记为准；当前为占位值。
PUBLIC_API_URL=http://localhost:8425/crate-api

# 理论培训（merit）后端 service-prototype 服务基址：
# 默认 127.0.0.1:8423，统一前缀 /crate-api/prototype/v1。
PUBLIC_MERIT_API_HOST=127.0.0.1
PUBLIC_MERIT_API_PORT=8423
