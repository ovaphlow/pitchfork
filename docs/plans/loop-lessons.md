# Loop 流程类教训

> 本文件记录自动化流水线各轮中暴露的**非代码缺陷**流程问题（测试命令语义、交接遗漏、状态更新等），供后续轮次避免重犯。代码缺陷走正常 findings 通道，不记录于此。

## 2026-08-02 · 评审轮 t_b4d03b34（docs-notes-hello-kanban2）

- 实施计划的「最终状态」由评审角色负责更新为「已完成」，但该字段改动若只留在工作区而不提交，会遗留脏工作区，阻塞后续 `git checkout main && git merge --no-ff <branch>`；且上一轮（t_520a5aee，提交 391728b）批准通过后计划仍停留在「待实施」，最终状态未落入 git 历史。教训：评审通过时，除交付清单外，须将计划状态字段的改动一并提交到分支（`docs(plan)` 提交），保持分支干净、状态可追溯。
