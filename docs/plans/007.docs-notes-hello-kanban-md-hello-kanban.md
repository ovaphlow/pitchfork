# `docs/notes/hello-kanban.md` 实施计划

状态：待实施

## 1. 目标与范围

创建 `docs/notes/hello-kanban.md`，其内容精确为一行文本 `hello-kanban`。

### 明确不在范围

- 不在本计划阶段创建 `docs/notes/hello-kanban.md`；该文件由开发角色实施。
- 不修改后端、前端、服务配置、数据库、Flyway 迁移、API 或其他文档。
- 不新增测试、依赖、脚本、提交或分支操作。

## 2. 可验证的验收口径

1. `docs/notes/hello-kanban.md` 存在，且为本任务新增文件。
2. 文件内容精确为单行 `hello-kanban`，不含额外文本、空白行或其他字符。
3. `git diff -- docs/notes/hello-kanban.md` 仅显示新增的该行内容。
4. `git diff --name-only` 仅包含 `docs/notes/hello-kanban.md`。

## 3. 小步实施步骤

1. **开发**：创建不存在的目录 `docs/notes/`。
2. **开发**：新增 `docs/notes/hello-kanban.md`，写入精确内容 `hello-kanban`。
3. **开发**：执行第 5 节的文件内容检查与 diff 检查，交付结果。
4. **测试**：只读复验文件存在、内容精确性及变更范围；不修改文件。
5. **评审**：根据开发与测试交付包确认范围、验收证据和无副作用后，更新最终状态。

## 4. 涉及文件清单

| 文件 | 用途 | 可修改角色 |
|---|---|---|
| `docs/notes/hello-kanban.md` | 新增单行看板说明 | 开发 |
| `docs/plans/007.docs-notes-hello-kanban-md-hello-kanban.md` | 本实施计划与状态记录 | 计划、评审 |

测试和评审均只读，不得修改 `docs/notes/hello-kanban.md`。本任务之外的任何文件均不在各角色的可修改范围内。

## 5. 测试清单

本任务为纯文档任务，无需单元测试、集成测试、前端构建、数据库测试或数据库连接。

在仓库根目录执行以下只读验证：

```bash
test -f docs/notes/hello-kanban.md
test "$(sed -n '1p' docs/notes/hello-kanban.md)" = "hello-kanban"
test "$(sed -n '2p' docs/notes/hello-kanban.md)" = ""
git diff --check
git diff -- docs/notes/hello-kanban.md
git diff --name-only
```

前两项确认文件与首行内容，第三项确认不存在第二行内容；后三项确认无空白错误、目标文件内容和变更范围。

## 6. 风险

- `docs/notes/` 当前不存在：开发必须先创建该目录。
- 并行分支合入可能同时创建或修改目标文件：发生冲突时仅解决该文件的内容归属，并重新执行第 5 节验证。
- 编辑器自动添加额外空白、空行或非预期文本会导致内容不精确：以第 5 节的逐行检查为准。

## 7. 角色、职责与交接

参与角色：计划、开发、测试、评审。

| 工作项 | 计划 | 开发 | 测试 | 评审 |
|---|---|---|---|---|
| 本计划、范围和验收口径 | 编写、维护 | 不修改 | 只读提出可测性建议 | 审查 |
| 创建目标目录和文档 | 不修改 | 实现 | 只读复验 | 只读审查 |
| 文件内容与 diff 验证 | 定义命令 | 执行并交付 | 独立复验 | 审查证据 |
| 最终状态 | 不宣告验收通过 | 不更新 | 不更新 | 唯一更新者 |

各角色的可修改范围以第 4 节为准：开发仅可修改 `docs/notes/hello-kanban.md`；计划和评审仅可修改本计划文件；测试没有可修改文件范围。测试命令为第 5 节命令，在本地工作区执行；不需要服务、网络、数据库授权、测试数据或清理操作，且不得连接任何数据库。

开发交付包必须包含修改文件、实际执行命令、退出状态及 `git diff --name-only` 输出。测试交付包必须包含复验命令、退出状态和确认的变更文件列表。若失败，测试按“前置条件、最小复现命令、预期结果、实际结果、脱敏证据”格式回传；开发仅修复目标文档后重新交付，测试只复验受影响项。评审在交付包完整且所有验收口径满足后，作为最终状态更新者更新本计划状态。
