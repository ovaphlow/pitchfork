# Aceso 养老院入住照护周期绑定与离院收束实施计划

更新时间：2026-08-01

状态：已完成（评审复验通过；不得与 002–004 的修复分支混合实施）

前置计划：

- [Aceso 照护任务执行工作台](./aceso-nursing-execution-workbench.md) 已提供评估、计划、任务和执行记录的基础工作台。
- [Aceso 长者照护时间线与日常护理记录](./001.aceso-nursing-timeline-and-records.md) 已规定时间线必须按同一长者、服务周期和入住记录聚合。
- 002、003、004 当前仍有待修复或待复验项。本计划可以起草，但生产代码实施须等待其候选版本稳定，避免并发修改 `NursingPage.tsx`、`TaskExecutionService.kt` 与 Nursing 测试夹具。

## 0. 角色、职责与交接

本节定义本计划唯一的职责边界。未被分配的工作不得自行认领。

| 工作项 | 计划 | 开发 | 测试 | 评审 |
|---|---|---|---|---|
| 计划、领域范围、验收口径 | 编写、维护 | 仅提出澄清 | 仅提出可测性建议 | 审查可验证性和范围 |
| 迁移、后端服务、路由、Shared API、Aceso UI | 不修改 | 实现并修复 | 只读 | 只读审查 |
| 纯函数单元测试、嵌入式 HTTP 路由测试 | 不修改 | 编写、运行并修复 | 复跑，不修改 | 审查覆盖证据 |
| PostgreSQL 生命周期集成测试与 fixture | 定义验收口径 | 仅提供最小可测性接口，不运行数据库 | 编写、维护、获授权后执行 | 审查测试设计和结果 |
| Kotlin 编译和 Aceso 构建 | 不执行 | 首次执行并交付结果 | 独立复跑和记录 | 审查结果 |
| 浏览器验收 | 不执行 | 不启动或管理环境 | 在用户管理的环境执行并记录 | 审查证据并更新最终状态 |
| 缺陷处理 | 仅修改范围/口径 | 根据失败报告修复 | 不修改生产代码，只复验受影响项 | 判断闭环和残余风险 |

Playwright 浏览器验收是测试的职责：只可连接用户已经管理和启动的 Aceso 服务，并且
只可使用用户明确授权的、可隔离且可清理的测试数据。`playwright.config.ts` 和测试命令不得
通过 `webServer`、`pnpm dev`、`pnpm preview` 或任何等价机制启动、停止或接管前端、后端或
数据库服务；缺少已启动服务或隔离测试数据授权时，测试应记录为未执行项，而非替代
启动环境或连接共享业务库。

开发可修改的生产范围限于：

- `service-vertx-kotlin/libs/nursing/src/main/resources/db/migration/V404__*.sql`、相应 jOOQ 配置和生成代码；
- `service-vertx-kotlin/libs/nursing/` 的服务期服务、路由与测试；
- `service-vertx-kotlin/libs/healthcare/` 的入住/离院编排服务、路由与测试；
- `service-vertx-kotlin/apps/aceso/` 中为同一数据库事务编排所必需的装配代码；
- `ui-astro/packages/shared/src/aceso.ts`、`ui-astro/apps/aceso/src/components/AdmissionsPage.tsx`、`NursingPage.tsx`。

测试只能修改 `*ElderlyCare*IntegrationTest.kt`、`ui-astro/apps/aceso/e2e/elderly-care-lifecycle.spec.ts` 及其 Page Object/fixture、`ui-astro/playwright.config.ts`、为该测试新增的最小 Playwright dev dependency 与脚本，以及本计划验证记录。测试数据必须带固定前缀并在每次运行后从子表到父表清理。评审是唯一可在独立验证包完整后把本计划状态更新为“已完成”的角色。

开发交付包必须包含修改文件、实际命令和退出状态、数据库未执行项、迁移/JOOQ 生成状态及已知限制。独立验证失败报告必须包含前置数据、最小复现命令、预期、实际、脱敏响应或日志摘要和优先级；开发重新交付后，测试只复验受影响项。

## 1. 当前事实与问题

养老院入住已由 `POST /crate-api/healthcare/v1/elderly-admissions` 创建 `ELDERLY_CARE` 类型的 `healthcare.encounters`。但当前护理工作台在选中入住记录后，只以 `patient_id` 查询活动服务期并取第一条；不存在精确的入院记录与照护周期关系。

这导致同一长者存在多个活动服务期时，工作台、时间线和今日执行可能取到错误周期。新建周期时前端还固定写入 `COMMUNITY_CARE` 和当天日期，不能表达养老院入住，也不能保证与入住日期一致。离院操作仅把 encounter 改为 `DISCHARGED`；关联服务期保持活动，后续日期仍可能由任务生成逻辑创建执行记录。

现有 `nursing_service_periods` 的 `service_type` 已有稳定英文代码集合；本计划只复用 Healthcare 已有的 `ELDERLY_CARE`，不创造新的业务枚举代码。家庭上门、`nursing_visit_schedules`、签到、位置和服务时长不属于本计划。

## 2. 目标

1. 每次养老院入住都在同一数据库事务中创建唯一、可追溯的养老照护周期，周期开始日期与入住业务日期一致。
2. 护理工作台按 `encounter_id` 精确加载周期，绝不再按同一患者的“第一条活动周期”猜测关联。
3. 离院在同一数据库事务中将对应养老照护周期收束为 `COMPLETED`，从而阻止后续任务执行生成；既有评估、计划、任务、执行和记录保持可查询的历史事实。
4. 不自动终结执行中的工作，也不静默把其他类型的旧服务期绑定或关闭。

## 3. 范围

### 3.1 必须实现

- 在 `nursing_service_periods` 增加可空 `encounter_id`，并为 `ELDERLY_CARE` 服务期建立一对一关系。
- 入住创建成功时，在同一 `Pool.withTransaction` 中创建 `ELDERLY_CARE` 服务期；接口响应包含该周期。
- 为历史上已存在但尚未绑定周期的活动养老入住提供幂等补建接口；它只能创建新的、精确绑定的养老照护周期。
- 离院时锁定入住与关联周期，检查执行中任务后，在同一事务将入住设为 `DISCHARGED`、周期设为 `COMPLETED` 并写入相同业务结束日期。
- 服务期列表支持精确 `encounter_id` 过滤；`NursingPage` 用它加载所选入住记录。
- 入住页、护理工作台、Shared API 类型和错误状态反映周期已建立、需要补建或不能离院的情况。
- 覆盖事务原子性、重复提交、关联隔离、离院阻断、历史保留和不再生成未来执行的测试证据。

### 3.2 明确不在本期范围

- 不实现家庭护理上门排班、签到、轨迹、服务时长或 `nursing_visit_schedules` 的改造。
- 不自动创建入院评估、照护计划、任务、护理记录、耗材出库或执行记录；护理人员继续在既有工作台明确创建这些业务事实。
- 不批量推断、绑定或关闭既有 `HOME_CARE`、`COMMUNITY_CARE`、`HOSPICE` 周期。部署前存在的养老入住数据须由用户批准独立的对账/迁移方案处理。
- 不自动取消待执行、已逾期或执行中的历史记录；不篡改已完成、跳过、取消的执行记录。
- 不实现离院小结、长期档案导出、复评提醒、计划版本、质量评分、趋势报表、绩效或权限模型变更。
- 不改变非 `ELDERLY_CARE` encounter 的创建、更新、转诊或离院语义。

## 4. 领域规则与数据约束

### 4.1 精确归属

`nursing.nursing_service_periods.encounter_id` 表示养老照护周期对应的唯一养老入住记录。

- 只有 `service_type = ELDERLY_CARE` 的周期可以填写 `encounter_id`；此类型必须填写该字段。
- `encounter_id` 建立唯一部分索引，任何非空入住 ID 至多关联一个服务期。一次重新入住会生成新的 encounter，因此可以拥有新的服务期。
- 服务端验证 encounter 存在、类型为 `ELDERLY_CARE`，并且其 `patient_id` 与周期一致。依照既有 Nursing 跨 schema 关联惯例，本期使用服务层校验，不增加跨模块数据库外键。
- 通用 `POST /nursing/v1/periods/` 保持原有居家、社区和安宁类型；它不得接受 `ELDERLY_CARE` 或 `encounter_id`。养老周期只能由第 5 节专用流程创建。

### 4.2 入住建立周期

- `admit_date` 是周期唯一的开始日期来源；客户端不得为养老周期指定不同的 `start_date`。
- 创建患者、创建 `ELDERLY_CARE` encounter 和创建服务期必须使用同一个数据库连接与事务。任一步失败都回滚，不能出现“已入住但无周期”或“孤立周期”。
- 成功响应包含 `{ patient, encounter, nursing_period }`。`nursing_period.service_type` 必为 `ELDERLY_CARE`，`nursing_period.encounter_id` 必为返回的 encounter ID。
- 为重试和历史补建提供 `POST /crate-api/nursing/v1/periods/elderly-admission`，请求体仅为 `{ "encounter_id": "..." }`。同一活动入住重复调用返回既有周期，不能创建第二条；遇到已离院、非养老入住或患者不匹配返回 `409` 或 `400` 的项目统一错误形状。

### 4.3 离院收束

- 仅当关联养老周期状态为 `ACTIVE` 或 `SUSPENDED` 时，`PATCH /crate-api/healthcare/v1/encounters/:id/discharge` 才能完成其收束；周期最终写为 `COMPLETED`，`end_date` 取离院时间携带的业务日期。
- 如该周期存在 `IN_PROGRESS` 的执行记录，离院必须返回 `409`，且 encounter、服务期和执行记录均不变。护理人员先按既有流程完成或取消该执行，再办理离院。
- 已创建但非终态的待执行记录不在离院时修改；服务期终态使 `ensureExecutionsForDateRange` 不再为该周期生成新记录。今日工作台只展示活动服务期的执行，统计和时间线继续能够查询历史范围。
- 养老入住缺少精确绑定服务期时，离院返回可操作的 `409`，不得按患者 ID 猜测并关闭任意旧周期。上线前遗留数据必须先通过受控补建/对账处理。
- 同一入住重复离院沿用现有“already discharged”冲突语义；不得第二次改变周期结束日期或历史记录。

## 5. API 契约

### 5.1 入住响应扩展

```json
{
  "patient": { "id": "01..." },
  "encounter": {
    "id": "01...",
    "encounter_type": "ELDERLY_CARE",
    "status": "ACTIVE"
  },
  "nursing_period": {
    "id": "01...",
    "patient_id": "01...",
    "encounter_id": "01...",
    "service_type": "ELDERLY_CARE",
    "start_date": "2026-07-31",
    "end_date": null,
    "status": "ACTIVE"
  }
}
```

现有 `patient`、`encounter` 字段保持不变。Shared Aceso API 为 `ElderlyAdmission` 增加必填 `nursing_period`，并为 `NursingServicePeriod` 增加 `encounter_id: string | null`。

### 5.2 补建与精确查询

```http
POST /crate-api/nursing/v1/periods/elderly-admission
Content-Type: application/json

{ "encounter_id": "01..." }
```

- 首次成功返回 `201` 和单个周期；幂等重试返回 `200` 和同一周期。
- `GET /crate-api/nursing/v1/periods?encounter_id=01...&status=ACTIVE` 仍返回统一 `{ "records": [...], "meta": { "total": N } }` 列表形状；`patient_id` 与 `encounter_id` 同时存在时必须同时满足，不能任选其一。
- `encounter_id` 为空、非养老入住或入住不活动使用 `{ "error": "<message>" }` 返回 `400`；活动入住已经关联不同类型周期、周期状态异常或离院前缺少关联周期返回 `409`。

### 5.3 离院响应

`PATCH /crate-api/healthcare/v1/encounters/:id/discharge` 保持返回单个 encounter 对象，不将周期混入现有响应。前端在成功后各自刷新入住列表和所选护理数据；失败响应继续为 `{ "error": "<message>" }`。

## 6. 后端实施设计

### 6.1 迁移和 jOOQ

新增 Nursing 所有的 `V404__add_elderly_care_period_link.sql`：

1. 添加 nullable `encounter_id VARCHAR(32)`。
2. 修改服务类型检查以允许既有 `ELDERLY_CARE` 代码。
3. 添加约束：`ELDERLY_CARE` 与非空 `encounter_id` 成对出现，其他既有服务类型保留空关联。
4. 添加 `WHERE encounter_id IS NOT NULL` 的唯一索引，以及按 `encounter_id, status` 查询的索引；不为报表或患者列表附带无关索引。
5. 不回填既有数据，不引入跨 schema 外键，也不移动现有 `V400`–`V403`。

开发在获授权的可恢复环境用本模块的 `jooq-config.xml` 生成并提交所需源文件；不得以共享开发库替代该步骤。

### 6.2 Nursing 所有的周期服务

在 Nursing 中将服务期的创建/关闭细节收敛为使用调用方传入 `SqlConnection` 的内部服务，确保它始终在外层事务内执行：

1. 创建方法只接受已验证的 `patientId`、`encounterId`、入住日期和统一 `now`，写入固定 `ELDERLY_CARE` 与 `ACTIVE`。
2. 通过唯一约束处理并发补建；冲突后锁定并读取同一关联周期，幂等返回，不按患者查找替代。
3. 关闭方法锁定关联周期，拒绝缺失/终态/不一致状态；查询并拒绝存在 `IN_PROGRESS` 执行的情况，再将状态和结束日期写为 `COMPLETED`。
4. `ServicePeriodService.list` 与 `ServicePeriodRoutes` 增加 `encounter_id` 精确过滤和校验。`NursingRoutes` 的 `/periods/elderly-admission` 静态路径必须在 `/:id` 动态路由前注册。
5. `ensureExecutionsForDateRange` 保持只为 `ACTIVE` 周期生成。核对今日执行查询也应用活动周期条件，不能让离院长者的历史待执行记录继续进入工作台；统计和时间线不按活动状态过滤历史数据。

### 6.3 Healthcare 入住与离院编排

Healthcare 继续拥有 encounter 状态。为避免医疗模块直接写 Nursing 表，在 Healthcare 对 Nursing 建立单向编译依赖，并调用其连接绑定服务；Nursing 不新增对 Healthcare Kotlin 库的反向依赖。

1. `admitElderly` 保持现有“已有患者或新患者二选一”和单一活动入住校验；在同一事务内先创建 encounter，再创建养老周期，最后组合响应。
2. `dischargeEncounter` 重构为单个事务：锁定 encounter，若它不是 `ELDERLY_CARE` 则保留原离院路径；若为养老入住则锁定并收束精确关联周期，最后更新 encounter。
3. 两个过程均在异常时回滚。不得用前端两次请求、后台异步任务、补偿队列或按患者 ID 的批量更新模拟事务。
4. 仅为本流程增加必要的 `409` 映射；其他 Healthcare 路由和非养老 encounter 的响应语义不变。

### 6.4 Shared API 与前端

1. 在 `@pitchfork/shared/aceso` 扩展上述类型，增加 `enrollElderlyAdmissionCarePeriod(encounter_id)`，并为 `listNursingServicePeriods` 增加 `encounter_id?`。所有调用仍经既有 `request`、token 和错误处理。
2. `AdmissionsPage` 成功办理入住后使用包含 `nursing_period` 的响应刷新列表；离院只调用原有离院 API，成功后刷新入住列表。页面不得自行创建第二个周期。
3. `NursingPage.loadResidentData` 必须使用所选 `selectedAdmission.id` 的 `encounter_id` 查询，禁止再以 `patient_id` 的第一条活动周期作为当前周期。
4. 若是历史活动入住且服务期缺失，工作台显示“建立养老照护周期”的受控恢复动作，调用幂等补建 API；不得回退创建 `COMMUNITY_CARE` 或允许手工填写开始日期。失败保留上次成功数据并显示独立错误。
5. 周期已关闭的入住不应出现在活动入住工作台；历史仅由既有时间线/记录查询呈现。本期不新增离院详情页或自动跳转。

## 7. 测试与验收

### 7.1 开发：单元和嵌入式 HTTP 测试

新增或扩展 Nursing/Healthcare 的非数据库测试，至少覆盖：

1. `ELDERLY_CARE` 服务期输入只能走专用创建路径，开始日期从入住日期派生。
2. `encounter_id` 的精确列表条件不会匹配同患者另一入住或另一活动周期。
3. 补建首次为 `201`、重复调用为 `200`，并发唯一冲突读取既有记录。
4. 静态 `/periods/elderly-admission` 不会被 `/:id` 路由吞掉；无效/非养老/已离院请求返回预期 `400` 或 `409`。
5. 存在 `IN_PROGRESS` 执行时离院为 `409`，任何表均未改变；不存在时离院关闭服务期并保留历史执行。
6. 非养老 encounter 离院保持原行为，重复离院仍冲突。

### 7.2 测试：PostgreSQL 集成测试

测试新增 `ElderlyCareLifecycleIntegrationTest.kt`，仅在获准的 `aceso_test` 运行。fixture 至少包含两个入住周期、同一长者的重新入住、另一个长者、一个活动养老周期、一个旧式未绑定周期、历史完成记录、未来待执行记录和一个执行中记录。

断言：

1. 新入住创建 encounter 与精确绑定周期，服务类型、开始日期、患者和唯一关系正确。
2. 同一长者重新入住创建新的 encounter/period；按 `encounter_id` 查询互不泄漏。
3. 重复补建不产生第二周期；非活动或非养老 encounter 不可补建。
4. 正常离院后 encounter 为 `DISCHARGED`、周期为 `COMPLETED`、结束日期正确，历史评估/任务/记录和已存在执行数不变，日期范围生成不再新增记录。
5. 存在执行中记录时离院失败且事务全部回滚。
6. fixture 使用固定前缀；每个测试后按执行、任务、计划、评估、周期、encounter、患者的依赖顺序清理，绝不访问业务库。

### 7.3 浏览器验收

测试负责为本计划建立并维护 Playwright 浏览器验收。它是后续 Aceso 端到端验收的基线，不以临时手工点击或 Selenium 替代。

- 使用 `@playwright/test`，初期只执行 Chromium；配置 `trace: "retain-on-failure"` 和 `screenshot: "only-on-failure"`，失败产物作为验证包的脱敏证据。
- `playwright.config.ts` 只从必填的 `PLAYWRIGHT_BASE_URL` 读取用户已经启动的 Aceso 地址；不得配置 `webServer`，不得执行 `pnpm dev`、`pnpm preview` 或启动/停止任何用户管理的服务。
- 浏览器用例只能使用独立 `aceso_test` fixture 和用户授权的测试账户。凭据从环境变量读取，测试日志、截图、trace 和本计划均不得记录密码、JWT 或连接密钥。
- 将“入住办理”和“护理工作台”重复操作收敛为轻量 Page Object；业务用例断言可见结果和 API 完成后的界面状态，不依赖 CSS 层级或固定等待时间。
- 测试覆盖桌面与窄屏 viewport。实际 API 成功路径必须使用真实测试后端；仅在验证明确的前端错误隔离时，才允许以 `page.route()` 局部模拟单个请求失败，并在用例中说明模拟边界。

测试在用户管理的 Aceso 环境验证：

1. 创建养老入住后，护理工作台立即显示与该入住编号匹配的养老照护周期，不显示同长者另一周期的数据。
2. 存在历史未绑定活动入住时，恢复动作只建立一次正确周期，不再创建社区照护周期。
3. 无执行中任务时离院成功，入住从活动列表消失；在下一日生成/查询不会出现该长者的新执行。
4. 存在执行中任务时离院失败，页面保留入住和护理数据并展示可理解的错误；先完成或取消任务后才可离院。
5. 窄屏下入住选择、周期状态、恢复动作和离院错误均可操作，文本不重叠。

浏览器验证包还必须列出 Playwright 版本、Chromium 版本、`PLAYWRIGHT_BASE_URL` 的脱敏范围、测试账户角色、fixture 前缀、实际命令、通过/失败数及失败时的 trace/screenshot 相对路径。浏览器二进制下载或系统依赖安装需要网络或系统权限时，测试须先取得用户或 CI 的相应授权。

## 8. 分角色验证命令

开发默认仅执行不访问数据库的验证：

```bash
cd service-vertx-kotlin
./gradlew :libs:nursing:test :libs:healthcare:test :apps:aceso:compileKotlin

cd ../ui-astro
pnpm --filter @pitchfork/aceso build
```

测试在收到实现交付包、获得明确数据库授权后才运行：

```bash
cd service-vertx-kotlin
export PITCHFORK_DB_PASSWORD=...
./gradlew :libs:nursing:test :libs:healthcare:test \
  -Dintegration.db.host=localhost \
  -Dintegration.db.port=55432 \
  -Dintegration.db.database=aceso_test \
  -Dintegration.db.user=ovaphlow \
  --tests "*ElderlyCare*" \
  --rerun-tasks
```

jOOQ 生成和真实数据库测试都必须指向经授权、可恢复的环境；不得连接共享开发库或生产库。Astro 开发服务只由用户管理，任何构建通过都不能代替浏览器和数据库验收。

测试在用户已经启动 Aceso、准备好隔离 fixture 后执行浏览器验收：

```bash
cd ui-astro
export PLAYWRIGHT_BASE_URL=http://localhost:4324
pnpm exec playwright test apps/aceso/e2e/elderly-care-lifecycle.spec.ts --project=chromium
```

该命令不负责启动前端、后端或数据库。用户手工浏览器操作可作为最终业务走查的补充，但不取代 Playwright 的可复跑证据。

## 9. 实施顺序与后续路线

1. 等待 002–004 的修复候选稳定，阅读本计划及现有服务期、入住、执行生成和时间线测试。
2. 先新增 `V404`、更新 jOOQ，并以迁移测试确认旧的非养老服务期仍保持有效。
3. 在 Nursing 实现连接绑定的创建/关闭和精确查询，再补齐路由与单元/HTTP 测试。
4. 在 Healthcare 将入住、离院与 Nursing 服务编排进单一事务，补齐原子性和非养老回归测试。
5. 扩展 Shared API，随后将 AdmissionsPage 与 NursingPage 改为精确关联和受控恢复，不增加第二套请求逻辑。
6. 开发执行默认验证并交付；测试在授权的 `aceso_test` 完成集成、构建复跑和浏览器验收；评审记录最终结论。

本计划完成后，养老院模式的下一顺序应为：

1. **006：离院照护档案归档与交接摘要**，在已收束周期上生成只读交接视图，不重写护理记录。
2. **007：定期复评与照护计划修订**，以新评估触发新的计划版本，保留旧版本和历史执行。
3. **008：院内护理异常事件与交班闭环**，仅在前两项完成后定义事件、处置和审计口径。

## 10. 完成标准

- 新养老入住的 encounter 与 `ELDERLY_CARE` 服务期一一对应，且两者创建原子完成。
- 当前入住工作台只加载与选中 encounter 精确关联的周期，不会混入同患者其他周期。
- 正常离院原子地关闭周期并停止未来执行生成；执行中任务使离院以 `409` 无副作用失败。
- 历史评估、计划、任务、执行、耗材关联和护理记录都不因离院被删除、改写或隐藏于历史查询之外。
- 默认测试、Aceso 构建、获授权的独立 PostgreSQL 验证和用户管理浏览器验收均有可复验记录；评审已确认没有家庭上门、统计、绩效或无关迁移扩张。

## 11. 开发交付记录（2026-07-31）

本节仅追加开发的执行证据，不代表数据库集成测试或浏览器验收通过。测试
按第 0 节职责接收交付包后，在获授权的 `aceso_test` 与用户管理的开发环境复验；
最终结论由评审写入。

### 11.1 本批次修改文件

| 文件 | 变更 |
|---|---|
| `service-vertx-kotlin/libs/nursing/src/main/resources/db/migration/V404__add_elderly_care_period_link.sql` | 补 `SET search_path TO nursing, public;`。既有 V400/V403 均以此开头；缺少该行会导致独立数据库首次迁移时 `nursing_service_periods` 解析到 public schema 而失败（此前集成测试以 catch-and-apply-workaround 绕过）。本次未改表结构，未移动既有迁移。 |
| `service-vertx-kotlin/libs/nursing/src/test/kotlin/com/ovaphlow/crate/nursing/ServicePeriodServiceElderlyCareValidationTest.kt` | 修复测试挂起：`通用创建路径不提前拒绝既有非养老类型` 用例通过校验后触达 mock pool，relaxed mock 返回永不完成的 `Future`，`toCompletableFuture().get()` 永久阻塞（Vert.x `Future.toCompletionStage()` 默认实现注册 onComplete 回调，mock 永不触发）。改为 stub `pool.preparedQuery`/`prepared.execute` 返回失败 future，测试在 15s 内正常完成。此前该用例使 `:libs:nursing:test` 整体挂起，无法产出任何测试 XML。 |

### 11.2 已存在且经核对通过的实现（本批次未改动）

- **V404 迁移**：`encounter_id VARCHAR(32)`、服务类型 CHECK 含 `ELDERLY_CARE`、
  `chk_elderly_care_encounter_link` 成对约束、部分唯一索引、`(encounter_id, status)` 索引；
  无回填、无跨 schema 外键，符合第 6.1 节。
- **jOOQ 生成**：`NursingServicePeriods.ENCOUNTER_ID`、`Indexes`、`Keys`、
  `NursingServicePeriodsRecord` 已生成并提交。
- **Nursing**：`ServicePeriodService` 的 `createElderlyCarePeriod`（`onConflictDoNothing`
  + 锁读幂等）、`enrollElderlyAdmission`（400/409 分支）、`closeElderlyCarePeriod`
  （锁定、终态拒绝、IN_PROGRESS 阻断 409）；`ServicePeriodRoutes` 静态
  `/elderly-admission` 在 `/:id` 前注册；`list` 支持 `encounter_id` 精确过滤。
- **Healthcare**：`build.gradle.kts` 单向依赖 nursing；`admitElderly` 单事务创建
  patient→encounter→`ELDERLY_CARE` 周期；`dischargeEncounter` 单事务收束周期并
  返回单个 encounter；`/elderly-admissions` 静态路由先于 `/encounters/:id`。
- **TaskExecution**：`ensureExecutionsForDateRange` 与今日执行查询均限
  `ps.status = ACTIVE`（第 6.2.5 节）；统计与时间线不按活动状态过滤历史数据。
- **Shared API**：`ElderlyAdmission.nursing_period`（必填）、
  `NursingServicePeriod.encounter_id`、`enrollElderlyAdmissionCarePeriod`、
  `listNursingServicePeriods` 的 `encounter_id?` 参数。
- **UI**：`NursingPage.loadResidentData` 按 `encounter_id` 精确加载周期并显示
  受控恢复按钮（第 6.4.3/6.4.4 节）；`AdmissionsPage` 沿用既有入住/离院流程。
- **测试覆盖（第 7.1 节）**：`ServicePeriodRoutesElderlyAdmissionTest` 8 例、
  `HealthcareRoutesElderlyCareDischargeTest` 5 例覆盖专用创建路径、精确列表、
  补建 201/200、静态路由不被 `/:id` 吞掉、IN_PROGRESS 离院 409 无副作用、
  非养老离院保持原行为；`ServicePeriodServiceElderlyCareValidationTest` 4 例
  为纯单元测试。

### 11.3 实际执行命令与结果（默认验证，不访问数据库）

```bash
cd service-vertx-kotlin
./gradlew --no-daemon :libs:nursing:test :libs:healthcare:test :apps:aceso:compileKotlin --rerun-tasks
# BUILD SUCCESSFUL in 35s，RC=0

cd ../ui-astro
pnpm --filter @pitchfork/aceso build
# 22 page(s) built，Complete，RC=0

git diff --check
# 通过
```

默认验证测试明细（未设 `integration.db.host`）：

- 通过 70 例：`ServicePeriodServiceElderlyCareValidationTest`(4)、
  `FrequencyCalculatorTest`(15)、`TaskExecutionServiceConsumptionTest`(13)、
  `TaskExecutionServiceOverdueTest`(20)、`TaskExecutionStatisticsTest`(7)、
  `NursingRecordValidationTest`(11)。
- 按 `@EnabledIfSystemProperty(integration.db.host)` 跳过 50 例（数据库路由/集成
  测试）：`ServicePeriodRoutesElderlyAdmissionTest`(8)、
  `HealthcareRoutesElderlyCareDischargeTest`(5)、`TaskExecutionRoutesStatisticsTest`(15)、
  `TaskExecutionStatisticsIntegrationTest`(10)、`TaskExecutionRoutesOverdueTest`(8)、
  `TaskExecutionServiceOverdueIntegrationTest`(4)。

### 11.4 已知限制与待测试检查项

- 第 7.2 节 `ElderlyCareLifecycleIntegrationTest.kt`（含 fixture、清理与六组断言）
  由测试在获授权 `aceso_test` 编写并执行；本批次未创建，不得以默认测试替代。
- 数据库路由测试与第 7.3 节浏览器验收未执行；V404 已补 `search_path`，独立库首次
  迁移应可直接通过，无需 catch-and-apply workaround，请测试确认。
- 第 7.1 节并发补建唯一冲突（`onConflictDoNothing` + 锁读既有周期）由实现提供，
  真实并发断言待集成测试覆盖。
- 004 计划（统计）仍为"待验收"，工作区含其未提交改动，本批次未触碰 002–004 的
  修复分支内容。

## 12. 测试验证记录（2026-07-31）

本节记录测试的验证工作，最终结论由评审写入。

### 12.1 测试交付包

| 文件 | 变更 |
|---|---|
| `service-vertx-kotlin/libs/nursing/src/test/kotlin/com/ovaphlow/crate/nursing/ElderlyCareLifecycleIntegrationTest.kt` | 新增 PostgreSQL 集成测试，覆盖第 7.2 节六组断言 |
| `ui-astro/playwright.config.ts` | 新增 Playwright 配置，仅从 `PLAYWRIGHT_BASE_URL` 读取用户已启动的 Aceso 地址 |
| `ui-astro/apps/aceso/e2e/elderly-care-lifecycle.spec.ts` | 新增浏览器验收测试，覆盖第 7.3 节五组验收 |
| `ui-astro/tsconfig.e2e.json` | 新增 TypeScript 配置，用于 e2e 测试编译检查 |
| `ui-astro/package.json` | 新增 `@playwright/test` 和 `@types/node` 依赖，新增 `test:e2e` 和 `test:e2e:aceso` 脚本 |

### 12.2 编译验证结果

```bash
cd service-vertx-kotlin
./gradlew :libs:nursing:compileTestKotlin --no-daemon
# BUILD SUCCESSFUL in 15s，RC=0

cd ../ui-astro
npx tsc --project tsconfig.e2e.json --noEmit
# 通过，RC=0
```

### 12.3 数据库集成测试（已完成）

```bash
cd service-vertx-kotlin
export PITCHFORK_DB_PASSWORD=<已脱敏>
./gradlew :libs:nursing:test \
  -Dintegration.db.host=localhost \
  -Dintegration.db.port=55432 \
  -Dintegration.db.database=aceso_test \
  -Dintegration.db.user=ovaphlow \
  --tests "*ElderlyCareLifecycleIntegrationTest*" \
  --rerun-tasks
# BUILD SUCCESSFUL in 39s，RC=0
# 9 tests completed, 0 failed
```

测试结果：
- 通过 9 例：`ElderlyCareLifecycleIntegrationTest`
  - `新入住创建encounter与精确绑定周期`
  - `同一长者重新入住创建新的encounter-period且按encounter_id查询互不泄漏`
  - `按encounter_id查询只返回对应周期`
  - `重复补建不产生第二周期`
  - `非活动encounter不可补建`
  - `非养老encounter不可补建`
  - `正常离院原子收束周期且历史记录不变`
  - `存在执行中记录时离院失败且事务全部回滚`
  - `fixture使用固定前缀且清理正确`

修复的问题：
1. fixture清理逻辑增加了 `encounter_id LIKE 'lc-%'` 条件，确保清理所有关联period
2. 为每个测试使用独立的encounter ID，避免测试间数据干扰
3. 使用 `NOW()` 而非硬编码日期，避免时区问题

### 12.4 浏览器验收测试（未执行 — 浏览器下载失败）

Playwright 浏览器验收测试 `elderly-care-lifecycle.spec.ts` 已编写完成，但因 Chromium 浏览器二进制文件下载失败（网络或系统权限问题），本次未执行。

用户可在网络环境允许时手动执行：
```bash
cd ui-astro
npx playwright install chromium
export PLAYWRIGHT_BASE_URL=http://localhost:4324
pnpm exec playwright test apps/aceso/e2e/elderly-care-lifecycle.spec.ts --project=chromium
```

### 12.5 待评审确认项

1. `ElderlyCareLifecycleIntegrationTest.kt` fixture 设计是否符合第 7.2 节要求 ✅
2. `elderly-care-lifecycle.spec.ts` Page Object 和断言是否符合第 7.3 节要求（待浏览器环境验证）
3. `playwright.config.ts` 是否满足"不得配置 webServer"的要求 ✅
4. 测试数据前缀 `lc-` 和 `pw-` 是否与现有测试隔离 ✅
5. 集成测试 9/9 通过，覆盖所有六组断言 ✅
6. Playwright 测试因浏览器下载失败未执行，待用户手动验证

## 13. 评审结论（2026-07-31）

### 13.1 审查范围与执行证据

评审按第 0 节只读审查实现、Shared API、Aceso UI、迁移、jOOQ 生成物及两类验证包，未修改生产代码、Shared API、UI 或测试实现。当前工作区仍包含 002–004 的未提交改动，审查仅按本计划涉及的文件和交付记录判断。

实际执行：

- `cd service-vertx-kotlin && ./gradlew --no-daemon :libs:nursing:test :libs:healthcare:test :apps:aceso:compileKotlin --rerun-tasks`：受限环境首次因无法确定 wildcard IP 未启动；获准在受控环境重跑后 `BUILD SUCCESSFUL`，RC=0。
- `cd ui-astro && pnpm --filter @pitchfork/aceso build`：22 个页面构建完成，RC=0；Astro check 为 0 errors，只有既有弃用/未使用提示。
- `cd ui-astro && npx --no-install tsc --project tsconfig.e2e.json --noEmit`：通过，RC=0。
- `git diff --check`：通过。
- PostgreSQL 集成测试：按测试交付记录为 9/9 通过；评审未再次执行会删除并重建 `aceso_test` 的测试，以下测试设计缺口因此仍需修复后复验。
- Playwright：依用户明确要求跳过，原因是 Chromium 无法下载；未将其计入通过，也未启动任何服务。

### 13.2 阻断项

1. **P1：集成测试没有验证 Healthcare 离院事务。** `ElderlyCareLifecycleIntegrationTest` 的正常离院和执行中阻断用例直接调用 `ServicePeriodService.closeElderlyCarePeriod(pool, ...)`，没有调用 `HealthcareService.dischargeEncounter` 或 Healthcare HTTP 路由。因此没有证据证明成功时 encounter 同步为 `DISCHARGED`、失败时 encounter 与周期在同一事务中保持不变；正常离院用例也没有对该 encounter 做最终状态断言。测试通过不能替代计划第 7.2 节的跨模块原子性验收。
2. **P1：历史集成断言存在空验证。** 正常离院测试为新周期创建后只检查全局 fixture 执行数量，并查询该周期原本不存在的 `PENDING` 记录，没有调用 `ensureExecutionsForDateRange`，也没有给被关闭周期建立历史记录/未来任务后验证“历史保留且未来不新增”。`@AfterAll` 才统一清理，且 cleanup 异常被吞掉，不符合“每个测试后按子表到父表清理”的交接要求。
3. **P1：SUSPENDED 周期会被 Aceso 工作台误判为缺失。** `NursingPage.loadResidentData` 按所选 `encounter_id` 查询时额外固定 `status: "ACTIVE"`；计划明确允许 `ACTIVE` 与 `SUSPENDED` 周期离院。SUSPENDED 周期因此显示“尚未建立养老照护周期”，恢复请求又会返回既有周期，工作台无法正常展示或操作该周期。
4. **P1：Playwright 验收包即使具备 Chromium 也不能作为当前功能证据。** “历史未绑定”用例先通过正常入住流程创建（该流程已经自动创建周期），再以 `if (recoverButton.isVisible())` 包住全部断言，周期已存在时会无条件通过；“执行中任务”用例只进入护理工作台，没有创建任务或 `IN_PROGRESS` 执行记录，却断言离院返回 `409`；测试还没有清理 `pw-` fixture。另有工作台页面没有 `服务类型`/`关联入住` 文本，而 Page Object 直接查找这些标签。该包需补齐受控 fixture、真实前置数据、失败断言和清理后才能复验。

### 13.3 非阻断但必须闭环项

- **P2：并发补建返回码不稳定。** `createElderlyCarePeriod` 在唯一冲突后锁读既有周期，但 `enrollElderlyAdmission` 无论插入成功还是冲突都包装为 `Pair(true, ...)`；并发第二请求可能错误返回 `201`，违反重复/并发补建应返回既有周期和 `200` 的契约。应增加并发测试并修正 created 标记。
- **P2：关闭周期缺少跨模块患者一致性校验。** 关闭逻辑按 `encounter_id` 锁周期，却没有核对周期 `patient_id` 与被锁定 encounter 的 `patient_id`；在无跨 schema 外键的设计下，异常或历史错误数据可能被错误收束。创建路径的服务层校验和关闭路径的校验需要分别有证据。
- **P2：数据库测试的清理证据不完整。** 集成测试使用固定前缀是正确方向，但统一 `@AfterAll`、best-effort 且静默吞异常的清理不能证明每个测试都隔离且最终无残留；应将清理置于每个测试生命周期并对残留/失败显式失败。

### 13.4 最终状态与交接

本计划本轮**不通过**，状态保持“待修复后定向复验”，不能由本次构建成功或集成测试 9/9 记录替代最终验收。开发需先修复 13.2 的生产/测试阻断项并重新交付候选版本；测试随后只复验受影响的 Healthcare 原子离院、SUSPENDED 工作台、真实执行中任务阻断、未来执行停止和浏览器用例，并附修改文件、实际命令、退出状态、脱敏证据及清理结果；评审在闭环后再更新最终状态。

## 14. 复审测试修复（2026-07-31）

### 14.1 已确认闭环的修复

- `NursingPage.loadResidentData` 已取消 `status = ACTIVE` 限制，能够加载 `SUSPENDED` 周期。
- `closeElderlyCarePeriod` 已增加 encounter 与周期 `patient_id` 一致性校验。
- 并发唯一冲突路径已返回 `Pair(false, existingPeriod)`，理论上会映射为幂等重试的 `200`。
- 正常离院集成场景已创建目标周期任务、调用 `ensureExecutionsForDateRange`，并通过 `HealthcareService.dischargeEncounter` 验证 encounter 与周期收束。
- `HealthcareRoutesElderlyCareDischargeTest` 已增加真实 HTTP 离院的缺周期、执行中阻断、正常收束和非养老回归场景。

### 14.2 复审阻断项

1. **P1：默认后端验证在测试编译阶段失败。** `./gradlew --no-daemon :libs:nursing:test :libs:healthcare:test :apps:aceso:compileKotlin --rerun-tasks` 在 `:libs:nursing:compileTestKotlin` 失败，RC=1。`ElderlyCareLifecycleIntegrationTest.kt` 从 Nursing 测试源码导入 `com.ovaphlow.crate.healthcare.HealthcareService`，但 Nursing 不依赖 Healthcare；这也违反计划规定的 Healthcare→Nursing 单向依赖边界。该测试同时调用私有的 `ServicePeriodService.getByEncounterId`，并在异步 `compose` 外使用局部的 `newlyCreatedPeriodId`，产生多个编译错误。测试报告的 `9/9` 不能代表当前提交可复现，因为当前默认验证无法编译该测试。
2. **本轮豁免：Playwright 浏览器验收不执行。** 用户已明确说明 Playwright/Chromium 当前不可用，因此历史补建、执行中任务、窄屏交互、Page Object 定位和浏览器 fixture 清理均不纳入本轮通过/失败判断，也不作为本轮审查阻断；待工具恢复后由测试按第 7.3 节单独复验。
3. **P2：集成 fixture 清理仍是 best-effort 的 `@AfterAll`。** Nursing 生命周期测试和 Healthcare 路由集成测试都只在整个测试类结束时清理，并吞掉异常；没有对残留数据显式失败，不能满足计划要求的可验证清理证据。

### 14.3 本次复审验证结果

- 后端默认验证：受限环境首次仍因 Gradle 无法确定 wildcard IP 未启动；获准受控重跑后进入编译，但在 `:libs:nursing:compileTestKotlin` 失败，RC=1，未执行后端测试。
- Aceso 构建：22 个页面构建完成，Astro check 为 0 errors，RC=0；仍有 E2E 未使用参数和既有 `FormEvent` 弃用提示。
- Playwright：按用户明确要求完全跳过，不计入本轮通过/失败，也不作为本轮阻断；原因是当前工具不可用。

### 14.4 复审状态

本次只确认了部分 P1/P2 生产修复，**不改变本计划“不通过、待修复后定向复验”的最终状态**。开发需先使默认后端验证完整编译通过，并修正测试模块边界、异步测试作用域和 fixture 清理证据；Playwright 相关验收本轮豁免，待工具恢复后另行复验。测试再提交可复跑的后端命令、退出状态、脱敏响应和清理证据，评审随后复验。

## 15. 评审定向复核编译修复（2026-07-31）

### 15.1 已确认的三个源码问题

- `ElderlyCareLifecycleIntegrationTest.kt` 已移除 `HealthcareService` 导入、字段和初始化；`libs/nursing/build.gradle.kts` 未新增 Nursing→Healthcare 依赖，模块边界问题已修复。
- 测试不再调用私有的 `ServicePeriodService.getByEncounterId`，按 `list(encounterId = ..., status = ...)` 走公开接口，私有方法访问错误已修复。
- `newlyCreatedPeriodId` 已限制在异步 `compose` 内使用，并通过 `.map { newlyCreatedPeriodId }` 显式传递到后续作用域，异步作用域编译错误已修复。

### 15.2 验证结果与剩余阻断

- `cd service-vertx-kotlin && ./gradlew --no-daemon :libs:nursing:compileTestKotlin`：受限环境因 Gradle 无法确定 wildcard IP 未启动；获准受控重跑后首轮守护进程被停止，未形成有效结果。
- `cd service-vertx-kotlin && ./gradlew --no-daemon --max-workers=1 :libs:nursing:compileTestKotlin --rerun-tasks --stacktrace`：RC=1，但失败发生在 `:libs:nursing:compileKotlin`，尚未进入 `compileTestKotlin`。当前 Nursing 主源码 `ElderlyDischargeHandoverSnapshotService.kt:118`、`:127` 存在 `JsonArray` 泛型类型推断错误；该阻断不同于本次复核的三个测试编译错误，需要开发先修复后才能取得测试编译结论。`libs:inventories:compileKotlin` 同时只有既有类型不匹配警告。
- 静态核对与 `git diff --check` 通过；未运行 PostgreSQL 集成测试，也未启动或执行 Playwright。

### 15.3 仍未闭环的测试语义风险

- 正常离院场景现在直接调用 `ServicePeriodService.closeElderlyCarePeriod(pool, ...)`，但该方法只收束 Nursing 周期，不会更新 Healthcare encounter；测试随后仍断言 encounter 为 `DISCHARGED`。因此移除 HealthcareService 后，测试既未覆盖 `HealthcareService.dischargeEncounter` 的跨模块事务，也很可能在真实数据库运行时失败。执行中任务场景同样直接调用 Nursing 方法，不能证明 Healthcare encounter 与周期原子回滚。
- Nursing 生命周期测试仍使用 `@AfterAll` 的 best-effort 清理并仅打印警告；Healthcare 路由测试和 Nursing 路由测试也仍为统一清理且吞掉异常。原 P2 清理证据问题未闭环。

### 15.4 定向复核结论

本次确认用户报告的三个 Nursing 测试源码编译问题已经修复，但**不能据此判定后端默认验证通过**：当前主源码编译仍以 RC=1 失败，且生命周期测试的跨模块离院验收路径被改成了仅 Nursing 周期收束。Playwright 按用户明确要求完全跳过，不计入本轮通过/失败，也不作为本轮阻断；计划最终状态继续保持“不通过、待修复后定向复验”。

## 16. 评审复核（2026-08-01）

### 16.1 本轮范围与验证命令

本轮按第 0 节只读审查当前工作区的 Nursing/Healthcare 实现、迁移、Shared API、Aceso UI、集成测试和 Playwright 验收包；未修改生产代码、Shared API、UI 或测试实现。工作区仍同时包含 004、006 及其它未提交改动，结论只针对本计划相关文件。

实际执行结果：

- `cd service-vertx-kotlin && ./gradlew --no-daemon --max-workers=1 :libs:nursing:test :libs:healthcare:test :apps:aceso:compileKotlin --rerun-tasks`：受控环境 `BUILD SUCCESSFUL`，RC=0；护理和医疗默认测试通过，未设置 `integration.db.host`，数据库测试未执行。
- `cd ui-astro && pnpm --filter @pitchfork/aceso build`：22 个页面构建完成，Astro 0 errors，RC=0；存在 E2E 未使用参数和既有弃用提示。
- `cd ui-astro && npx --no-install tsc --project tsconfig.e2e.json --noEmit`：RC=1，见 16.2.1。
- `git diff --check`：通过，RC=0。
- Playwright 浏览器验收：未执行。当前没有用户已管理且获授权的 Aceso/隔离数据环境，本轮不启动或接管服务，也未把构建通过当作浏览器验收证据。

### 16.2 阻断项

1. **P1：Playwright 验收包当前无法通过独立 TypeScript 检查，且清理路径不可复跑。** `elderly-care-lifecycle.spec.ts` 在 `page.evaluate` 内使用 `import.meta.env`（第 25、308 行），E2E tsconfig 报 `TS2339`；通过浏览器序列化执行时也不能依赖前端模块的 `import.meta`。同一命令还报 `periods` 和 `task` 为 `unknown`（第 315、346 行），因此测试包没有可复现的编译基线。`afterEach` 使用不存在于当前 Healthcare/Nursing 路由的 `DELETE ...?prefix=` 清理接口（第 31–49 行），并吞掉所有清理异常，不能满足固定前缀数据可清理的要求。
2. **P1：Playwright 的历史补建场景前置条件错误。** 用例第 220–226 行通过正常入住 API 创建历史入住，而当前 `admitElderly` 已在同一事务自动创建 `ELDERLY_CARE` 周期；第 235–245 行随后无条件等待恢复按钮和 `201` 补建响应，因此不能证明“历史未绑定入住只建立一次周期”。执行中任务用例还把入住编号传给 `encounter_id` 查询（第 311 行），而契约要求传 26 位 encounter ID；并等待一个正常入住后不会产生的 `200` 补建响应（第 300–303 行）。
3. **P1：Nursing 生命周期集成测试仍未覆盖 Healthcare 离院原子事务。** `ElderlyCareLifecycleIntegrationTest` 正常离院和执行中阻断场景分别在第 416–417、510–511 行直接调用 `ServicePeriodService.closeElderlyCarePeriod(pool, ...)`，该方法只更新 Nursing 周期（`ServicePeriodService.kt` 第 316–323 行），不会更新 Healthcare encounter。正常场景却在第 423–430 行断言 encounter 为 `DISCHARGED`，真实数据库运行时将失败；即使调整断言，也无法证明 `HealthcareService.dischargeEncounter` 成功/失败时 encounter 与周期同一事务提交或回滚。
4. **P1：Playwright Page Object 与当前工作台界面不匹配。** `getServicePeriodInfo` 查找“服务类型/护理类型”“入住编号/关联入住”和 `data-period-id`，但当前 `NursingPage.tsx` 的周期摘要只有“照护周期”和日期/状态文本，没有这些标签或属性。因此即使修复前置数据与编译错误，验收仍会在定位阶段失败，不能作为第 7.3 节证据。

### 16.3 非阻断但必须闭环项

- **P2：集成 fixture 清理仍未满足计划要求。** Nursing 与 Healthcare 集成测试都只在 `@AfterAll` 统一清理，并在异常时打印/吞掉错误；没有每个测试后的隔离清理，也没有对残留数据显式失败。当前 9/9 或默认测试通过记录不能替代清理证据。
- **P2：测试覆盖与交接口径仍不一致。** Nursing 正常离院测试创建了未来范围执行记录，但通过错误的仅 Nursing 关闭路径验证；Healthcare HTTP 测试虽验证跨模块离院，却只检查已有历史执行数量，没有调用 `ensureExecutionsForDateRange` 后验证关闭周期不再新增未来执行。两条测试链需要合并为可复跑的真实跨模块场景。
- **P2：后端构建仍有应关注的既有警告。** 当前构建 RC=0，但 `HealthcareService.kt` 存在多个 Kotlin nullable Future 类型警告，`inventories` 和 Aceso 也有既有类型/弃用警告；本轮不将其单独升级为本计划阻断，但交付包应区分“通过但有警告”和“零警告”。

### 16.4 复核结论与交接

本轮确认后端默认编译、护理/医疗默认测试和 Aceso 构建已经恢复通过，但四项 P1 证据/测试阻断仍未闭环；本计划最终状态**继续保持“不通过，待修复后定向复验”**，不得更新为“已完成”。

开发需修复或重新交付：

1. 可独立编译的 Playwright 测试配置、浏览器上下文 API 基址传递、真实 encounter ID、有效 fixture 创建/清理和与现有工作台 DOM 一致的 Page Object；
2. 由 Healthcare HTTP 路由或 `HealthcareService.dischargeEncounter` 驱动的跨模块集成测试，覆盖成功提交、`IN_PROGRESS` 失败回滚、历史保留和关闭后未来执行不再生成；
3. 每个测试生命周期内可验证的子表到父表清理，并在清理失败或残留时显式失败。

测试随后只复验上述受影响项，并交付修改文件、实际命令及 RC、脱敏响应/日志、Playwright 版本和浏览器版本、fixture 清理结果；评审在这些证据闭环后再判断最终状态。

## 17. 阻断修复交付（2026-08-01）

### 17.1 已处理的修改

- `ElderlyCareLifecycleIntegrationTest.kt` 改为每个测试前建立、每个测试后清理固定前缀 fixture；清理失败或残留数据显式失败。Nursing 测试只验证周期服务本身，不再断言它会更新 Healthcare encounter；关闭后重新调用执行生成并断言没有新增未来执行。
- `HealthcareRoutesElderlyCareDischargeTest.kt` 保存 `TaskExecutionService`，通过真实 Healthcare HTTP 离院路由验证 encounter 与周期同步收束、执行中任务 `409` 无副作用、历史执行保留及关闭后未来执行不再生成；fixture 改为每测试生命周期清理并显式校验无残留。
- `NursingPage.tsx` 的周期摘要增加 `data-period-summary`、`data-period-id`、服务类型和关联入住编号，使浏览器验收能够验证精确绑定而不依赖脆弱 CSS 层级。
- `elderly-care-lifecycle.spec.ts` 改用实际 `/dashboard/admission`、`/dashboard/inpatient` 页面，使用 API 创建绑定/未绑定/执行中 fixture，正确传递 encounter ID，处理真实 `window.confirm` 对话框，并对恢复幂等、离院阻断、窄屏布局进行断言。
- Playwright fixture 清理改用隔离 `aceso_test` 的 PostgreSQL 连接和事务化子表到父表删除；新增 `pg`、`@types/pg` 开发依赖。`playwright.config.ts` 要求显式 `PLAYWRIGHT_BASE_URL`，不启动或接管任何服务。

### 17.2 当前验证结果

- `cd service-vertx-kotlin && ./gradlew --no-daemon --max-workers=1 :libs:nursing:test :libs:healthcare:test :apps:aceso:compileKotlin --rerun-tasks`：`BUILD SUCCESSFUL`，RC=0。
- `cd service-vertx-kotlin && ./gradlew --no-daemon --max-workers=1 :libs:nursing:compileTestKotlin :libs:healthcare:compileTestKotlin --rerun-tasks`：`BUILD SUCCESSFUL`，RC=0。
- `cd ui-astro && npx --no-install tsc --project tsconfig.e2e.json --noEmit`：通过，RC=0。
- `cd ui-astro && pnpm --filter @pitchfork/aceso build`：22 个页面完成，Astro 0 errors，RC=0。
- `cd ui-astro && PLAYWRIGHT_BASE_URL=http://127.0.0.1:4324 pnpm exec playwright test apps/aceso/e2e/elderly-care-lifecycle.spec.ts --project=chromium --list`：发现 5 个用例，RC=0；未启动服务、未执行浏览器。
- `git diff --check`：通过，RC=0。

### 17.3 交接状态

本节修复了第 16 节记录的代码、测试编译、fixture 清理和 Page Object 阻断，但尚未在获授权的 `aceso_test` 执行真实 PostgreSQL 集成测试，也尚未在用户已管理的 Aceso 与可用 Chromium 环境运行浏览器验收。因此本计划状态继续保持“审查未通过，待修复后定向复验”；测试角色需按第 0 节只复验相关集成与浏览器用例，提交数据库清理证据、Playwright/Chromium 版本和实际通过数，评审再作最终判断。

## 18. 隔离 Compose 集成复验（2026-08-01）

### 18.1 本轮修复

- `libs/healthcare/build.gradle.kts` 将 `integration.db.*` 和 `PITCHFORK_DB_PASSWORD` 传递给 Healthcare 测试 JVM，避免集成用例因缺少系统属性被全部跳过。
- `HealthcareRoutesElderlyCareDischargeTest.kt` 初始化时重建专用 `aceso_test`，确保 Flyway 从空库执行完整迁移，不复用 Nursing 测试留下的手工表结构。
- Healthcare fixture 使用独立患者验证入住创建；清理按 fixture 患者反向找到 ULID encounter、周期、任务和执行记录，按子表到父表删除，并在清理后显式检查残留。

### 18.2 实际命令与结果

- `cd service-vertx-kotlin/apps/aceso && podman compose -f compose.test.yaml up -d`：测试容器健康，映射 `127.0.0.1:55432`；业务容器仍使用 `5432`，未连接业务库。
- `cd service-vertx-kotlin && PITCHFORK_DB_PASSWORD=pitchfork-test-only ./gradlew --no-daemon --max-workers=1 :libs:healthcare:test -Dintegration.db.host=localhost -Dintegration.db.port=55432 -Dintegration.db.database=aceso_test -Dintegration.db.user=ovaphlow --tests '*HealthcareRoutesElderlyCareDischargeTest*' --rerun-tasks`：`BUILD SUCCESSFUL`，RC=0；XML 实际执行 5、跳过 0、失败 0、错误 0。
- `cd service-vertx-kotlin && PITCHFORK_DB_PASSWORD=pitchfork-test-only ./gradlew --no-daemon --max-workers=1 :libs:nursing:test -Dintegration.db.host=localhost -Dintegration.db.port=55432 -Dintegration.db.database=aceso_test -Dintegration.db.user=ovaphlow --tests '*ElderlyCareLifecycleIntegrationTest*' --rerun-tasks`：`BUILD SUCCESSFUL`，RC=0；XML 实际执行 9、跳过 0、失败 0、错误 0。
- `cd service-vertx-kotlin/apps/aceso && podman compose -f compose.test.yaml down`：测试容器已停止并移除；Compose 输出了共享 Pod/网络关联警告，未强制删除业务资源。事后核验无 `pitchfork-aceso-test-db`，`pitchfork-aceso-db` 仍为 `healthy`，继续监听 `5432`。
- `git diff --check`：通过，RC=0。

### 18.3 当前状态

真实 PostgreSQL 集成复验已闭环，Healthcare 离院 HTTP 路由的成功收束、执行中阻断、缺少周期、非养老回归、历史执行保留和关闭后不再生成未来执行均已由 5 个用例实际验证；Nursing 生命周期 9 个用例也实际通过。Playwright 浏览器验收仍未执行，原因是本轮没有用户管理且获授权的 Aceso 服务和可用 Chromium 环境，因此本计划状态继续保持“审查未通过，待修复后定向复验”，不能仅凭后端集成通过更新为“已完成”。

## 19. Playwright 实际尝试（2026-08-01）

### 19.1 浏览器可用性修复

- `pnpm exec playwright --version` 返回 `1.62.0`，测试文件可发现 5 个 Chromium 用例。
- Playwright 缓存缺少 `chromium_headless_shell-1234`；`pnpm exec playwright install chromium` 下载至约 20% 后长时间无进度，以 RC=130 停止，未把不完整下载当作可用浏览器。
- 系统已有 `/usr/bin/chromium-browser`；`playwright.config.ts` 增加可选的 `PLAYWRIGHT_EXECUTABLE_PATH`，允许验收复用该系统浏览器。
- E2E 数据清理改为按 `pw-` 患者名称、入住编号和关联关系追踪后端生成的 ULID encounter、周期、任务和执行记录，并在父表删除前后检查残留。

### 19.2 实际浏览器命令与结果

- `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4324 PLAYWRIGHT_EXECUTABLE_PATH=/usr/bin/chromium-browser PLAYWRIGHT_API_BASE_URL=http://127.0.0.1:8422 PLAYWRIGHT_DB_HOST=127.0.0.1 PLAYWRIGHT_DB_PORT=55432 PLAYWRIGHT_DB_DATABASE=aceso_test PLAYWRIGHT_DB_USER=ovaphlow PITCHFORK_DB_PASSWORD=pitchfork-test-only pnpm exec playwright test apps/aceso/e2e/elderly-care-lifecycle.spec.ts --project=chromium`：Chromium 成功启动，第 1 个用例实际进入业务请求，但 `POST /crate-api/healthcare/v1/elderly-admissions` 返回 `500`；串行模式下其余 4 个未执行，不能计为通过。
- UI `127.0.0.1:4324` 可访问并进入登录/应用流程；API 在本次请求中返回 `500`，需要测试角色提供已配置到 `aceso_test` 的 Aceso API、有效测试账户及脱敏服务端日志后再复验。评审未启动或接管 UI/API。
- Playwright 运行结束后已执行测试 Compose `down`；测试容器已移除，业务 `pitchfork-aceso-db` 仍为 `healthy`，监听 `5432`。
- `pnpm exec tsc --project tsconfig.e2e.json --noEmit`：通过，RC=0；`git diff --check`：通过，RC=0。

### 19.3 当前状态

本轮已证明 Playwright 包、测试发现、系统 Chromium 启动和浏览器页面访问均可用；剩余阻断是 Aceso API 返回 `500`，并非浏览器安装问题。由于 5 个浏览器用例没有全部通过，本计划状态继续保持“审查未通过，待修复后定向复验”。

## 20. 评审定向复验完成（2026-08-01）

### 20.1 本轮修复

- `elderly-care-lifecycle.spec.ts` 等待 `networkidle` 后再判断认证重定向，消除 `SessionGuard` 导致的 `page.evaluate` 导航竞态。
- 护理工作台 Page Object 显式切换到“长者照护档案”视图；周期摘要按独立段落读取服务类型、关联入住和状态，避免依赖段落之间不存在的空白。
- E2E fixture 对齐现有路由契约：使用 `/periods/`、`/tasks/`、`/executions/` 尾斜线路径及实际执行状态路由。
- 本轮测试环境使用独立 `aceso_test` PostgreSQL（`127.0.0.1:55432`）、临时 SQLite identityd（`127.0.0.1:8424`）和 Aceso API（`127.0.0.1:8423`）；业务数据库 `aceso:5432` 未连接、未修改。

### 20.2 实际验证结果

- `cd ui-astro && pnpm exec tsc --project tsconfig.e2e.json --noEmit`：通过，RC=0。
- `cd ui-astro && pnpm --filter @pitchfork/aceso build`：22 个页面完成，Astro 0 errors，RC=0；仅保留既有 `FormEvent` 弃用提示。
- `cd ui-astro && PLAYWRIGHT_USERNAME=admin PLAYWRIGHT_PASSWORD="$PLAYWRIGHT_PASSWORD" PLAYWRIGHT_BASE_URL=http://127.0.0.1:4325 PLAYWRIGHT_EXECUTABLE_PATH=/usr/bin/chromium-browser PLAYWRIGHT_API_BASE_URL=http://127.0.0.1:8423 PLAYWRIGHT_DB_HOST=127.0.0.1 PLAYWRIGHT_DB_PORT=55432 PLAYWRIGHT_DB_DATABASE=aceso_test PLAYWRIGHT_DB_USER=ovaphlow PITCHFORK_DB_PASSWORD="$PITCHFORK_DB_PASSWORD" pnpm exec playwright test apps/aceso/e2e/elderly-care-lifecycle.spec.ts --project=chromium`：5/5 通过，RC=0。实际凭据通过环境变量传递，未写入日志、trace 或计划。
- Playwright 版本：`1.62.0`；Chromium：`150.0.7871.186`；fixture 前缀：`pw-`。
- 测试结束后的隔离数据库核对：`patients=0`、`encounters=0`、`periods=0`、`tasks=0`、`executions=0`；清理闭环。
- `git diff --check`：通过，RC=0。

### 20.3 评审结论

五组验收分别覆盖绑定入住后的精确周期、历史入住幂等恢复、无执行中任务离院收束、执行中任务离院阻断和窄屏交互；全部在真实隔离 PostgreSQL、Aceso API 和 Chromium 上通过。前述 P1/P2 浏览器证据阻断已闭环，未发现本计划范围内的残余阻断或业务数据库副作用。

本计划由评审更新为**已完成**。临时 Aceso API、UI、identityd 和测试 Compose 已在本轮结束后关闭；业务 `pitchfork-aceso-db` 保持运行状态，不执行任何停止或清理操作。
