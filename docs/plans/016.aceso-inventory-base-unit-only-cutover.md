# Aceso 库存单一基础单位切换实施计划

更新时间：2026-08-07

状态：待实施（015 的包装换算候选已被替代；未授权修改生产代码、数据库或生成源）

相关决策：[ADR-001：库存以最小可发放／可消耗单位记账](../decisions/ADR-001-inventory-base-unit-of-measure.md)。
本计划在实施第 0 步完成 ADR 的相应收敛后，以本文件的范围和验收口径为准。

## 1. 决策、目标与术语

### 1.1 已确认的切换决策

产品尚未形成正式版本，Aceso 的开发数据库、候选迁移、候选 jOOQ 源和 API 均允许破坏性调整。后续
Inventory、Pharmacy 和 Nursing 的每一个业务数量、库存余额、锁定量、库存流水和单位成本都只使用物资
的**基础单位**。系统不再提供包装单位输入、包装到基础单位的换算、`PACKAGE` / `SPLIT` 枚举、默认
包装规格、`split_ratio` 或历史兼容投影。

“基础单位”是物资可合法发放／可实际消耗的最小业务单位，而不是物理上无限可分的最小颗粒：

- 阿莫西林可以定义为“片”，数量精度为 `0`；入库、发药、退药和护理消耗均提交片数；
- 允许体积分装的消毒液可以定义为“mL”，并由物资设置 `0..6` 位小数精度；
- 无菌最小包、不可回库的单支注射剂或整瓶药液可分别定义为“包、支、瓶”，数量精度通常为 `0`；
  这不是“整包模式”，而是该物资本身的基础单位。

采购供应商的包装文字、箱规或发票描述若未来确有需求，应由独立采购计划作为供应商/单据展示信息处理，
不得影响库存数量、成本、可用量、并发锁定或库存操作的口径。

### 1.2 目标终态

| 范围 | 终态 |
| --- | --- |
| 物资 | 一个 `base_unit` 和一个 `quantity_scale`；存在库存或确认事实后不可修改。 |
| 库存结存 | `stocks.quantity`、`stocks.locked_quantity` 直接表示基础数量；无并行 `base_quantity` 列。 |
| 库存流水 | `stock_operation_details.quantity`、`unit`、`unit_cost`、`total_cost` 都是基础数量/基础单位/每基础单位成本/总成本的不可变快照。 |
| 药房单据 | 发药、退药、申领、调拨、采购订单与收货的数量均为基础数量；单位由服务端从物资快照写入，不接受客户端选择。 |
| 护理耗材 | 依据 `stock_id` 确定物资和基础单位，只接受基础数量。 |
| API 与 UI | 不接受、不返回、不展示 `PACKAGE`、`SPLIT`、`package_unit`、`split_unit`、`split_ratio`、`unit_spec_id`、`input_quantity`、`conversion_ratio` 或 `split_quantity`。 |

### 1.3 明确不在本期范围

- 供应商档案、包装条码、箱规、采购价格协议、发票、供应商退货和采购退货；
- 盘点、账实调整、报损、召回、自动补货、成本报表或先进先出；
- 医嘱、发药、护理给药的状态机重构，或将发药与实际给药合并；
- 保留任何现有开发数据库的数据，或为历史 API 提供兼容端口；
- 修改 Trainova、共享服务或未经本计划列出的产品边界。

## 2. 角色、职责与交接

本节是本任务的最终职责边界。参与角色为计划、开发、测试、评审与用户业务确认；未被分配的工作不得
自行认领。创建或更新本计划不代表候选代码、数据库或浏览器验收通过。

| 工作项 | 计划 | 开发 | 测试 | 评审 | 用户业务确认 |
| --- | --- | --- | --- | --- | --- |
| 终态语义、范围与 ADR | 更新本计划并提出 ADR 收敛文本 | 提出实现约束 | 提出可测性约束 | 审查范围无遗漏 | 确认每类物资的基础单位和精度 |
| 未发布基线重建策略 | 定义只允许可丢弃数据库 | 实现迁移基线、生成源和默认验证 | 仅在授权 `aceso_test` 验证重建 | 审查无共享库/生产库操作 | 确认测试数据可清理 |
| Inventory 数据/API | 定义单一基础数量契约 | 改迁移、服务、路由、单元/路由测试 | 验证事务、精度、并发与回滚 | 审查无双写和无旧字段 | 提供合法单位样本 |
| Pharmacy 与 Nursing | 定义其数量/成本快照口径 | 改端口、服务、路由、单元/路由测试 | 验证发药、退药、申领、收货及耗材 | 审查状态机未越界 | 验证临床数量展示 |
| Shared API 与 Aceso UI | 定义破坏性 API 契约 | 改共享客户端、组件、构建 | 授权后浏览器验收 | 审查无直接请求和无遗留控件 | 手测业务用语 |
| 最终状态 | 记录范围变更 | 修复经复现的生产缺陷并重新交付 | 提供脱敏证据和清理结果 | 唯一可更新为“已完成” | 确认实际预期 |

### 2.1 可修改文件范围

- **计划**：本文件、[015](015.aceso-inventory-base-unit-and-packaging-conversion.md) 的替代状态，以及经用户
  基础单位确认后的 ADR-001；不修改生产代码、测试或数据库；
- **开发**：`libs/inventories` 的 V200/V201、服务、路由、jOOQ 配置与生成源、非数据库测试；
  `libs/pharmacy` 的 V300--V303、端口、服务、路由、非数据库测试；`libs/nursing` 的 V402/V403、
  服务、路由、非数据库测试；必要的 Aceso 挂载；`ui-astro/packages/shared/src/aceso.ts` 和
  Aceso 库存、药房、护理组件/路由；
- **测试**：只修改本计划新增或重命名为 `BaseUnit` / `BaseQuantity` 的 PostgreSQL 集成测试、fixture、
  Playwright 用例及验证记录；不得修改生产代码、Shared API、UI 或默认单元/路由测试；
- **评审**：只读审查实现和证据；仅可写入本计划的评审/验收记录和最终状态；
- **用户业务确认**：不修改仓库；确认单位、精度和不可拆分物资名单，提供脱敏手测结论。

每次交接必须附修改文件、实际执行命令、退出码、通过数、未执行项目和已知风险。测试失败报告必须提供
`aceso_test` 的可清理状态、前置数据、最小复现命令、预期、实际和脱敏证据；开发修复后重新交付，测试
仅复验受影响项，评审再作最终判断。

## 3. 实施门槛与破坏性切换规则

### 3.1 实施前必须满足

1. 用户业务确认每种物资的 `base_unit`、`quantity_scale` 和不可拆分规则，至少覆盖片/粒、支/瓶、
   液体、无菌组合包装与普通耗材；
2. ADR-001 删除“包装输入与换算快照”作为本期库存模型的一部分，明确所有写入均为基础数量；
3. 评审确认 015 候选的 V202/V407、`UnitConversionService`、包装端口和生成源不会进入待合并变更；
4. 测试提供独立、可恢复、可清理的 `aceso_test`。禁止连接共享开发库、业务库或生产库；
5. 开发和测试确认所有本地 Aceso 开发数据库都可重建。若有必须保留的数据，本计划停止，不以
   “尚未发布”为由覆盖或重写该数据。

### 3.2 数据库重建规则

本计划授权的是**未发布 Flyway 基线的重写**，不是对已有业务数据库做原地迁移：

- 开发在代码审查后修改 V200/V201、V300--V303、V402/V403，使其从空库直接创建单一基础单位模型；
- 开发删除 V202、V407 及其 jOOQ 生成的表/列，不创建 `MIGRATION_BLOCKED`、回填、包装规格或兼容列；
- 已应用旧校验和的本地数据库必须由其所有者或测试单命令入口删除并重建，不能通过 `outOfOrder`、
  手工更新 Flyway 历史或直接 UPDATE 业务表规避；
- 测试只在 `aceso_test` 验证“空库 -> 当前所有迁移 -> 当前 jOOQ/API”，并在报告中记录清理结果；
- 任何环境被发现包含需保留数据时，立即停止破坏性路径，由计划和用户重新决定迁移方案。

## 4. 数据与 API 契约

### 4.1 Inventory 基线

开发修改 `libs/inventories` 的 V200/V201：

| 对象 | 必须保留/新增 | 必须删除 |
| --- | --- | --- |
| `materials` | `base_unit VARCHAR NOT NULL`、`quantity_scale SMALLINT NOT NULL CHECK (0..6)`、状态、批次控制、成本方法和审计字段 | `package_unit`、`split_unit`、`split_ratio`、`unit_model_status`。 |
| `stocks` | `quantity NUMERIC(20,6)`、`locked_quantity NUMERIC(20,6)`、`total_cost NUMERIC(24,8)`，并约束 `quantity >= locked_quantity >= 0` | `base_quantity`、`locked_base_quantity` 及任何包装数量投影。 |
| `stock_operation_details` | `quantity NUMERIC(20,6)`、`unit`（基础单位快照）、`unit_cost NUMERIC(24,8)`、`total_cost NUMERIC(24,8)` | `split_quantity`、`unit_spec_id`、`input_quantity`、`input_unit`、`conversion_ratio`、`base_quantity`、`base_unit`、`input_unit_cost`、`base_unit_cost`。 |
| 计量规格/异常表 | 不保留 | `material_unit_specs`、`inventory_unit_migration_issues` 及相应约束、索引、jOOQ 表。 |

服务端从十进制文本构造 `BigDecimal`，数量精度以 `RoundingMode.UNNECESSARY` 验证，拒绝超精度而不静默
进位或四舍五入。库存锁定、可用量、批次、成本累计与所有操作均只读取和写入这一套数量。

### 4.2 写入请求和响应

新 API 不提供兼容格式。服务端对未知字段返回 `400`，不忽略旧字段：

| 用例 | 请求中允许的数量字段 | 服务端确定的单位/成本语义 |
| --- | --- | --- |
| 手工入库 | `items[].quantity`、`items[].unit_cost` | 数量是物资基础数量；成本是每基础单位成本。 |
| Pharmacy 发药/退药 | `dispensed_quantity` / `quantity`、必要的批次与业务字段 | 数量是基础数量；物资单位由服务端写入明细快照。 |
| Pharmacy 申领/调拨 | `requested_quantity` / `approved_quantity` | 全部是基础数量；预留、释放和转移同口径。 |
| Pharmacy 采购/收货 | `ordered_quantity` / `received_quantity`、`unit_cost` | 数量和单位成本均为基础口径。 |
| Nursing 耗材消耗 | `consumptions[].stock_id`、`consumptions[].quantity` | `stock_id` 确定物资、批次和基础单位；客户端不传单位、规格或换算率。 |

所有库存、物资、药房和护理响应只返回基础数量及基础单位。列表仍遵从
`{ "records": [...], "meta": { "total": N } }`；不保留包装投影字段。

## 5. 开发实施顺序

### 5.1 第 0 步：废弃候选并冻结目标

1. 开发从待合并集合删除 015 候选的 V202/V407、`UnitConversionService`、`MaterialUnitSpecs`、
   `InventoryUnitMigrationIssues` 及其 jOOQ 源；
2. 计划更新 ADR-001 和 015 替代说明，删除“历史兼容”和“包装输入”验收口径；
3. 评审确认分支中没有将旧候选与本计划混合的提交；测试不对 015 候选执行数据库验收。

### 5.2 第 1 步：重写未发布数据库基线并生成 jOOQ

1. 在 V200 中把物资单位模型改为 `base_unit + quantity_scale`；
2. 在 V201 中将库存和库存明细改为唯一基础数量模型，移除 `PACKAGE` / `SPLIT` 检查约束；
3. 在 V402/V403 中把护理耗材关联改为基础数量和基础单位快照，删除 `split_quantity` 与枚举约束；
4. 在 V300--V303 中把 Pharmacy 明细数量、单位成本和单位快照改为基础口径，删除默认/固定的
   `PACKAGE` 值、`SPLIT` 枚举和 `split_quantity`；
5. 删除 V202/V407；在授权的空 `aceso_test` 运行当前 Flyway 基线，再运行实际 `generateJooq`；
6. 提交真实生成源以及 `Keys`、`Indexes`、`Tables` 变更，不提交 `build/tmp` 或手工伪造的生成文件。

### 5.3 第 2 步：收敛 Inventory 服务、路由和单元测试

1. 将 `MaterialService` 的创建/更新契约改为一次性提交 `base_unit` 与 `quantity_scale`；删除
   `activateUnitModel`、规格 CRUD、默认规格和全部包装/拆零 JSON 字段；
2. 删除 `UnitConversionService`、`resolvePackagePort`、`resolveSplitPort`、`legacyDetailColumns`、
   `PackageInboundCommand`、`PackageOutboundCommand` 和所有 `*Package*` 库存服务方法；
3. 让 `StockService` 的入库、出库、预留、释放、调拨和采购收货只携带基础 `quantity` 与每基础单位成本，
   一次写入 `stocks.quantity`、`locked_quantity` 和流水数量；
4. 更新 `InventoriesRoutes`、`MaterialRoutes`、响应序列化及错误映射：旧字段一律 `400`，不存在过渡回退；
5. 用基础数量测试替换 `PACKAGE`、`SPLIT`、换算比、规格切换、双写和旧字段投影测试。

### 5.4 第 3 步：收敛 Pharmacy 的数量口径

1. 将 `PharmacyPorts` 的 `Package*` 数据类、接口方法和实现改为不含包装语义的库存端口；
2. 修改 `DispenseService`、`ReturnService`、`RequisitionService`、`PurchaseOrderService`：删除
   `unit == "PACKAGE"` 限制，数量、可退数量、预留量、订单累计量和成本均直接按基础单位处理；
3. 更新相应路由和 Shared API：客户端不可传 `PACKAGE`、`SPLIT` 或任意单位选择；
4. 保持现有医嘱有效性检查、幂等、同事务库存写入、退药上限、批次校验和状态机边界，不以单位切换
   顺带改变这些规则；
5. 更新非数据库测试，覆盖基础数量发药、部分退药、申领预留/释放/调拨和采购部分收货。

### 5.5 第 4 步：收敛 Nursing 耗材执行

1. 将 `TaskExecutionService.ConsumptionInput` 和 `InventoryConsumptionService.ConsumptionItem` 收敛为
   `stockId + quantity`；
2. 删除护理路由对 `unit`、`quantity`、`split_quantity` 和规格输入的互斥分支，只校验基础数量为正且
   适合该物资精度；
3. 护理耗材关联和关联库存操作明细必须在同一事务内写入相同的基础数量、单位和成本；
4. 保持任务状态流转、幂等重试和失败全事务回滚；测试以基础单位快照比较重复提交。

### 5.6 第 5 步：更新 Shared API 与 Aceso UI

1. 删除 `@pitchfork/shared/aceso` 中包装、拆零、规格和换算接口/类型；所有数值类型与实际 API 一致；
2. 库存页面只配置物资基础单位和精度，删除包装单位、默认规格、比率、规格停用和包装展示；
3. 入库、发药、退药、申领、采购和护理消耗表单均以基础单位为标签与校验依据；
4. 删除 Nursing 页面 `PACKAGE/SPLIT` 选择、包装预览和由包装可用量推导的提示；
5. 删除 Pharmacy 页面基于 `package_unit` 和 `unit === "PACKAGE"` 的筛选、显示与可退条件；
6. 页面必须仍通过 `@pitchfork/shared/aceso` 请求 API，且在窄屏显示长单位名、六位数量、批次和错误文本
   时不重叠。

### 5.7 第 6 步：清理、审查与交付

1. 删除无引用的旧测试、fixture、模拟行、生成表、端口命名和文档术语；不能用保留死代码证明兼容；
2. 执行默认验证并交付命令、退出码、测试数、生成源状态和未执行项目；
3. 测试在授权隔离环境完成第 6 节集成与浏览器验收；
4. 评审核对 API、迁移、数据模型和 UI 中不存在包装/拆零兼容入口后，才可更新最终状态。

## 6. 验证与验收

### 6.1 开发默认验证

开发只运行不访问数据库的检查：

```bash
cd service-vertx-kotlin
./gradlew :libs:inventories:test :libs:pharmacy:test :libs:nursing:test :apps:aceso:compileKotlin \
  --no-daemon -Pkotlin.compiler.execution.strategy=in-process --offline

cd ../ui-astro
pnpm --filter @pitchfork/aceso build

cd ..
git diff --check
```

默认测试至少覆盖：基础数量精度拒绝、单一库存可用量/锁定量、批次校验、基础单位成本、发药/退药、
申领预留/释放/调拨、采购累计收货、护理耗材同事务回滚、旧字段 `400`、未知字段白名单和 API 列表格式。

### 6.2 授权 PostgreSQL 集成验证

测试角色仅在用户提供可恢复的 `aceso_test` 后执行。第一项验收是空库从 V200/V201、V300--V303、
V400--V406 的当前基线完成迁移；不应存在 V202/V407 或包装规格表。fixture 至少包含：

| 物资 | 基础单位/精度 | 验证重点 |
| --- | --- | --- |
| 阿莫西林 | 片 / `0` | 入库 48 片、发药 5 片、退回 2 片，余额、成本与退回上限正确。 |
| 消毒液 | mL / `3` | 合法小数基础量、超精度拒绝和总成本稳定。 |
| 无菌敷料 | 包 / `0` | 只能按完整最小无菌包计数，不能借由小数绕过规则。 |
| 批次药品 | 片 / `0` | 同批次并发出库/锁定/释放不超发，批次与效期校验有效。 |

必须断言：

1. 任意库存写入和查询均只有一套基础数量，不存在包装投影、双写、换算率或 `PACKAGE/SPLIT`；
2. Pharmacy 发药、退药、申领、调拨、采购收货与 Nursing 消耗在同一事务内写入与库存一致的基础数量；
3. 失败时库存、锁定、流水、护理任务和 Pharmacy 单据均无半成品；
4. 并发同一库存行时满足 `quantity >= locked_quantity >= 0`，不产生裸 `500` 或重复库存行；
5. 旧字段、包装规格 ID、换算率和客户端伪造单位/成本字段得到 `400`，无任何写入；
6. 测试后按依赖顺序清理 fixture，并记录迁移版本、认证方式（不记录 token）、命令、断言和清理结果。

### 6.3 浏览器验收

在用户管理的 Aceso API、UI 与授权测试账户上执行：

1. 新建片剂物资，设置“片、0 位小数”，检查页面没有包装单位、比率或规格控件；
2. 录入 48 片并检查库存、可用量和成本均以片显示；尝试 0.5 片和旧包装字段，确认错误且库存不变；
3. 发药 5 片、退回 2 片、申领/调拨和采购收货，检查每张单据与库存均显示同一基础单位数量；
4. 护理耗材消耗只输入基础数量，详情与库存流水数量、单位、成本一致；
5. 窄屏检查长单位名、六位数量、批次号、错误信息不重叠，表格按需横向滚动。

## 7. 交付、回退与最终状态

开发交付包必须包含：基线迁移变化和 jOOQ 状态、删除的旧候选清单、API 破坏性变更清单、修改文件、
默认验证结果和未执行项。测试交付包必须补充 `aceso_test` 的重建、迁移、fixture、并发/回滚、清理和
浏览器证据。评审确认以下条件全部满足后，才可将本计划更新为“已完成”：

- ADR、迁移、API 和实现均明确只有基础单位口径；
- V202/V407、包装规格表、`PACKAGE/SPLIT`、旧输入字段、双写逻辑和旧 UI 控件均不存在；
- 默认验证、隔离 PostgreSQL 和 Chromium 验收均有可复验结果；
- 不存在 P1/P2 的数量精度、成本、并发、事务、药房/护理状态机或 API 合约缺陷；
- 所有测试数据已清理，且未连接或改动任何需要保留数据的数据库。

在本计划实施前的唯一回退方式是停止合并候选代码并重建可丢弃开发数据库；本计划不授权对已保留数据的
环境执行反向 Flyway、手工数据修复或破坏性覆盖。

## 8. 实施记录

| 日期 | 角色 | 项目 | 结果 | 说明 |
| --- | --- | --- | --- | --- |
| 2026-08-07 | 计划 | 016 创建 | 待实施 | 根据用户确认“尚未形成正式版本、允许破坏性改动、全面以基础单位为准”创建。未修改生产代码、迁移、jOOQ、数据库或测试。 |
