# Aceso 福利院药房退药与可追溯退库闭环实施计划

更新时间：2026-08-06

状态：待测试验收（开发候选版本已交付）

## 1. 背景与目标

011 的发药确认已经能够将福利院用药医嘱对应的包装库存出库，但已发药单不能直接取消，实际业务需要把未使用或错发的包装退回原仓库，并保留从原发药明细到库存回库操作的完整链路。本计划优先实现这一项独立业务能力，不等待 011 及更早计划完成最终验收。

目标是形成以下闭环：

```text
已发药明细 → 创建待确认退药单 → 锁定原物资/批次/成本 → 确认回库
                                          └→ Inventory INBOUND + PHARMACY_RETURN
```

## 2. 范围与约束

必须实现：

- 只允许从 `DISPENSED` 发药单的 `PACKAGE` 明细创建退药；患者、仓库、物资、批次、原出库明细和单位成本全部由服务端从原记录推导。
- 创建退药单时校验正数、退药原因、操作人和 `restockable = true`，并把 `PENDING`/`CONFIRMED` 退药量计入剩余可退数量。
- `PENDING` 退药确认必须在 Pharmacy 外层 `SqlConnection` 内调用库存回库端口，成功后回写退药库存操作明细、成本并置为 `CONFIRMED`。
- 库存回库使用既有 `INBOUND` 操作类型，操作元数据写入 `source = PHARMACY_RETURN`；库存行存在时累加数量和成本，不存在时创建。
- 确认接口幂等；已确认单重试不得重复增加库存；已确认单不可取消。
- Aceso 药房页面提供退药 Tab、创建退药、确认入库和取消待确认退药单入口，并复用 `@pitchfork/shared/aceso` API 客户端。

不在本期范围：拆零退药、退费、自动质检、跨仓调拨、采购/供应商流程、数据库集成验收和浏览器 E2E。

## 3. API 契约

路径前缀：`/crate-api/pharmacy/v1`。

```text
POST /returns/from-dispense
GET  /returns
GET  /returns/:id
PUT  /returns/:id/confirm
PUT  /returns/:id/cancel
```

创建请求只接受：

```json
{
  "dispense_id": "01J...",
  "dispense_item_id": "01J...",
  "quantity": 1,
  "return_reason": "老人未使用",
  "operator": "护士甲",
  "restockable": true,
  "remark": "包装完整"
}
```

服务端拒绝旧的通用 `POST /returns/` 入口，避免客户端伪造患者、仓库、物资、批次和单位成本。列表仍返回 `{ "records": [], "meta": { "total": 0 } }`，错误返回 `{ "error": "<message>" }`。

## 4. 实施进度

- [x] 增加 Pharmacy `InventoryInboundPort` 及 Aceso `Main.kt` 同连接适配器。
- [x] 增加 Inventory 包装回库服务：物资/批次/效期校验、库存锁定、入库操作、明细和结存更新。
- [x] 重构 `ReturnService` 创建、确认、取消和详情读取，完成剩余数量、状态和幂等校验。
- [x] 增加退药路由和 400/404/409 错误映射。
- [x] 增加 Shared API 客户端和 TypeScript 类型。
- [x] 增加 Aceso Pharmacy 退药 Tab 及创建/确认/取消页面流程。
- [x] 增加 Pharmacy/Inventory 非数据库单元测试。
- [x] 完成 Pharmacy、Inventory、Aceso Kotlin 编译和 Aceso 前端构建。
- [ ] 在授权的隔离数据库环境执行迁移与集成/E2E 验收。
- [ ] 由评审角色确认最终验收结论并更新状态。

## 5. 验证记录

已执行：

```bash
cd service-vertx-kotlin
rtk ./gradlew :libs:pharmacy:test :libs:inventories:test \
  --rerun-tasks --no-daemon \
  -Pkotlin.compiler.execution.strategy=in-process --offline
rtk ./gradlew :apps:aceso:compileKotlin \
  --no-daemon -Pkotlin.compiler.execution.strategy=in-process --offline

cd ../ui-astro
rtk pnpm --filter @pitchfork/aceso build
```

结果：Pharmacy 35 项、Inventory 47 项单元测试通过；Aceso Kotlin 编译通过；Aceso `astro check`、静态构建通过（仅保留既有 `FormEvent` 弃用警告）。测试未连接共享数据库，未宣称数据库或浏览器验收通过。

## 6. 角色、职责与交接

参与角色：计划、开发、测试、评审。

- 计划：维护本文件的范围、验收口径、状态和交接记录；可修改 `docs/plans/012.aceso-pharmacy-medication-return.md`，不得代替测试或评审宣告最终通过。
- 开发：实现 Pharmacy/Inventory 后端、Aceso Shared API/UI 及不访问数据库的单元测试；可修改 `service-vertx-kotlin/libs/pharmacy/**`、`service-vertx-kotlin/libs/inventories/**`、`service-vertx-kotlin/apps/aceso/**`、`ui-astro/packages/shared/src/aceso.ts`、`ui-astro/apps/aceso/src/components/PharmacyPage.tsx`；交付候选版本、修改文件和实际命令结果。
- 测试：只在授权的独立测试数据库 `aceso_test` 和可恢复浏览器环境执行本计划指定的集成/E2E；可修改测试 fixture、集成/E2E 测试和测试记录，不修改生产代码、Shared API、UI 或单元测试。失败回传必须包含前置数据、最小复现命令、预期/实际结果和脱敏证据。
- 评审：独立检查开发 diff、事务边界、伪造输入防护、测试证据和残余风险；可修改本计划的评审结论，不代替开发修复生产代码；只有评审可记录最终验收状态。

交接包：候选版本修改文件、上述实际执行命令及结果、未执行数据库/E2E 验收的原因和待测试项。测试发现缺陷后回传最小复现，开发修复后重新交付候选版本，测试只复验受影响项，评审再更新最终状态。
