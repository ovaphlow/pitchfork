API_KEY=change-me
STORAGE_DIR=./storage
# Option A: provide a full SQLALCHEMY DATABASE_URL (takes precedence if set)
#DATABASE_URL=postgresql+psycopg2://postgres:password@postgres:5432/auxfiles

# Option B: provide Postgres components and let the app build the URL
PG_USER=postgres
PG_PASSWORD=password
PG_HOST=postgres
PG_PORT=5432
PG_DB=auxfiles
